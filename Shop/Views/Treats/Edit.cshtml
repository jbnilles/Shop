@{
  Layout = "_Layout";
}

@model Shop.Models.Treat
<div class="container">
  <div class="jumbotron">
    <h1 class="display-4">
      Edit @Model.Name
    </h1>

  </div>
  <div class="card card-body">

    @using (Html.BeginForm())
    {
      @Html.HiddenFor(model => model.TreatId)

      @Html.LabelFor(model => model.Name)
      @Html.EditorFor(model => model.Name, new { @required = "required"})
      @Html.LabelFor(model => model.Description)
      @Html.EditorFor(model => model.Description)

      
      <input type="submit" value="Edit Treat" class="btn btn-outline-dark" />
      <button class="btn btn-outline-dark" type="button" onclick="window.location.href='/treats/details/@Model.TreatId';">Cancel</button>
    }
  </div>
</div>
  @Html.Partial("_Header")
  @Html.Partial("_Footer")

